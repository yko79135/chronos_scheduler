import { solveSchedule } from './solver.js';
import { NormalizedData, SolveOptions } from './types.js';
self.onmessage=(event:MessageEvent<{data:NormalizedData;options:SolveOptions;jobId:string}>)=>{const {data,options,jobId}=event.data;try{let last=0;const started=Date.now();const result=solveSchedule(data,{...options,now:()=>{const n=Date.now(); if(n-last>250){last=n;(self as unknown as Worker).postMessage({type:'progress',jobId,progress:{elapsedMs:n-started,nodes:0,bestScore:0,reason:'running'}});} return n;}});(self as unknown as Worker).postMessage({type:'result',jobId,result});}catch(error){(self as unknown as Worker).postMessage({type:'error',jobId,error:error instanceof Error?`${error.message}\n${error.stack??''}`:String(error)});}};
