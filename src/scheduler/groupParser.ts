const compact=(v:string)=>v.normalize('NFKC').replace(/\s+/g,'').replace(/[()]/g,'').trim();
export function gradeNumber(name:string):number|undefined{const m=String(name).match(/(?:G|Grade)?\s*(\d{1,2})|^(\d{1,2})학년/iu);return m?Number(m[1]??m[2]):undefined}
export function canonicalGradeName(input:string):string|undefined{const raw=String(input).trim();const n=gradeNumber(raw);if(n===undefined)return undefined;const c=compact(raw).toUpperCase();const section=c.match(/^G?(\d{1,2})(?:-?(\d+)?)?([EK])$/i);if(section?.[3])return `G${n}${section[3].toUpperCase()}`;return `G${n}`;}
export function parseGradeExpression(input:string, registered:string[]):string[]{const raw=compact(String(input));if(!raw)return[];if(/^(전학년|All)$/i.test(raw))return[...registered];const parts=raw.split(/[,+]/).filter(Boolean);const out=new Set<string>();for(const part of parts){const r=part.match(/(?:G|Grade)?(\d{1,2})\s*-\s*(?:G|Grade)?(\d{1,2})(?:학년)?/i);if(r){const a=Number(r[1]),b=Number(r[2]);registered.forEach(g=>{const n=gradeNumber(g);if(n!==undefined&&n>=Math.min(a,b)&&n<=Math.max(a,b))out.add(g)});continue}const canon=canonicalGradeName(part);const exact=registered.find(g=>compact(g).toLowerCase()===part.toLowerCase()||canonicalGradeName(g)===canon);if(exact)out.add(exact);else if(canon){const same=registered.filter(g=>canonicalGradeName(g)===canon);if(same.length===1)same.forEach(g=>out.add(g));else if(same.length===0){const n=gradeNumber(part);registered.filter(g=>gradeNumber(g)===n&&!/[EK]$/i.test(g)).forEach(g=>out.add(g));}}}return[...out];}
export function stableId(prefix:string,name:string):string{return `${prefix}_${name.normalize('NFKC').toLowerCase().replace(/[^\p{L}\p{N}]+/gu,'_').replace(/^_|_$/g,'')}`}
